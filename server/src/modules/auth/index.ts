/**
 * @module Auth
 * @description Auth feature module — placeholder.
 *
 * Clean Architecture structure:
 *   domain/          ← Entities, repository interfaces, domain errors
 *   application/     ← Use cases, DTOs
 *   infrastructure/  ← Mongoose repository implementations
 *   presentation/    ← HTTP controllers, routes, Zod validators
 *
 * Implementation will be added via a separate prompt.
 */

// Module barrel — export router when implemented
// export { authRouter } from './presentation/routes/auth.routes';
